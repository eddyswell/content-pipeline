---
name: Use python3 not python
description: User's system uses python3 as the command, not python
type: feedback
---

Always use `python3` instead of `python` when giving terminal commands to this user.

**Why:** Their system's PATH maps `python3` as the correct command.

**How to apply:** Any time I write a terminal command involving Python (running scripts, testing, pipeline runs), use `python3`.
